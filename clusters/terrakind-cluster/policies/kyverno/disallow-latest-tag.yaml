---
apiVersion: policies.kyverno.io/v1
kind: ValidatingPolicy
metadata:
  name: disallow-latest-tag
  annotations:
    policies.kyverno.io/title: Disallow Latest Tag
    policies.kyverno.io/category: Best Practices
    policies.kyverno.io/severity: medium
    policies.kyverno.io/subject: Pod
    policies.kyverno.io/description: >-
      The ':latest' tag is mutable and can lead to unexpected errors if the
      image changes. A best practice is to use an immutable tag that maps to
      a specific version of an application Pod. This policy validates that the image
      specifies a tag and that it is not called `latest`.
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["pods"]
  validations:
    - expression: >
        object.spec.containers.all(c, c.image.contains(':') && !c.image.endsWith(':latest'))
      message: "All containers must specify an image tag that is not 'latest'."
    - expression: >
        !has(object.spec.initContainers) ||
        object.spec.initContainers.all(c, c.image.contains(':') && !c.image.endsWith(':latest'))
      message: "All init containers must specify an image tag that is not 'latest'."
    - expression: >
        !has(object.spec.ephemeralContainers) ||
        object.spec.ephemeralContainers.all(c, c.image.contains(':') && !c.image.endsWith(':latest'))
      message: "All ephemeral containers must specify an image tag that is not 'latest'."
